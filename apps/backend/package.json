{
  "name": "@chronivs/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Chronivs V2 backend — FastAPI application",
  "scripts": {
    "dev": "cd ../../backend && .venv\\Scripts\\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "cd ../../backend && .venv\\Scripts\\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
